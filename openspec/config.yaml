schema: spec-driven

context: |
  Tech stack: NestJS for APIs, Next.js/React/Tailwind/shadcn for web frontend
  Storage: localStorage as cache, PostgreSQL via Next.js API routes
  API conventions: RESTful, JSON responses
  Testing: No testing, no unit tests, no integration tests, no end to end tests
  Style: ESLint with Prettier, strict TypeScript
  Build: Use pnpm workspace and node_modules located in 2 folders up (in a folder named 'doit_root') to resolve dependencies

rules:
  proposal:
    - Include rollback plan
    - Identify affected teams
  specs:
    - Use Given/When/Then format for scenarios
  design:
    - Include sequence diagrams for complex flows

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
